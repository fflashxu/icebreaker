services:
  - type: web
    name: icebreaker
    env: node
    buildCommand: npm run build
    startCommand: cd backend && npx prisma migrate deploy && node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
